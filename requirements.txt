# requirements.txt
# Core dependencies for the FastAPI server and asynchronous web service.

# FastAPI framework for building the API
fastapi

# Uvicorn for serving the FastAPI application asynchronously
uvicorn

# AI/LLM dependencies
langchain       # Framework for building LLM applications (RAG, chains, etc.)
openai          # Official Python client for OpenAI API calls
python-dotenv   # Library to load environment variables from .env file (for local testing)
langchain-openai
